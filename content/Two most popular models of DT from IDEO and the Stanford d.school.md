- [[6 stages design thinking process by IDEO]]
- [[5 stages design thinking process by Stanford d.school]]

In both the models, it starts with user(empathy) and ends with user(validation).

[[DT not a linear process from problem to solution but a process with series of divergent and convergent steps]]

But we need two more stages `inspire` which address the question of `Why design thinking?` and `scale` aims at ensuring that the `impact is realized to the idea's truest potential`